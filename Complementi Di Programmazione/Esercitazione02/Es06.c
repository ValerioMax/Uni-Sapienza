#include <stdio.h>

int main()
{
    int i = 10;
    int j = -1;
    char *p, *q;
    // Inserire codice qui (senza j = ...)
    *p = (int*) p;
    *q = (int*) q;
    printf("%d == %d\n", i, j);

    return 0;
}