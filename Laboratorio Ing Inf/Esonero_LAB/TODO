- data_line è l'input della funzione add() oppure il campo della classe Pydantic?

- add() deve gestire anche inserimenti parziali di tuple come ("film1", "regista1", 54) (solo tre campi) ?

- metti GRANT all permissions... per l utente non root nel DB

- se metto nel docker-compose image: mariadb:11.7.2-ubi9 funzionano gli heatlth check ma non crea i file nella cartella mariadb_data la prima volta che buildi (li crea solo se si fa chmod 0777 alla cartella mariadb_data)
    se metto image: mariadb:latest non vanno gli healthcheck ma crea il db anche senza dare i permessi con chmod 0777

- metti commenti con triplici apici python """ """ e commenti nel codice html ecc

- forse fare una tabella sola se no come si implementa l'inserimento di una nuova riga? bisognerebbe specificare la tabella dove la si mette

- se faccio solo docker-compose up non li runna in ordine i container