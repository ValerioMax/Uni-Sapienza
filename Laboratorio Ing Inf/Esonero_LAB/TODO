- E' meglio connettersi una volta sola al DB appena si esegue il server e disconnetersi quando lo si chiude
    o connettersi e disconnettersi ogni volta che si chiama un endpoint get o post

- Fixare il fatto che non si vedono piu di un risultato di un bottone alla volta (perche sono in context diversi e index.html ne trova uno alla volta)

- Devo ritornare TUTTE le colonne in ogni query (select *)? perche in alcune chiede selezione i titoli (devo fare select titolo oppure select * ?)
--> NON CREDO

- data_line è l'input della funzione add() oppure il campo della classe Pydantic?

- add() deve gestire anche inserimenti parziali di tuple come ("film1", "regista1", 54) (solo tre campi) ?

- Gestisci UPDATE in caso di cambio di campi per un regista (es eta) -> vedi slide 22

- I permessi chmod 0777 darli dal docker file e non metterli a mano perche il prof non li avrà ovviamente

- Fare una tabella per i film e una per i registi

- metti healthcheck nel docketcompose.yml (vedi se metterli o no)

- gestione errori ( input invalidi -> 422 (unprocessable entity (stringa in formato sbagliato)),
                    
                    )

- metti GRANT all permissions... per l utente non root nel DB

- se metto nel docker-compose image: mariadb:11.7.2-ubi9 non funzionano gli heatlth check ma non crea i file nella cartella mariadb_data la prima volta che buildi (li crea solo se si fa chmod 0777 alla cartella mariadb_data)
    se metto image: mariadb:latest non vanno gli healthcheck ma crea il db anche senza dare i permessi con chmod 0777

- metti commenti con triplici apici python """ """ e commenti nel codice html ecc

- fai script che parsa il file data.tsv e butta tutto nel DB